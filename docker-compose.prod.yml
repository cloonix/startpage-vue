# DEPRECATED: This file is kept for backwards compatibility
# Use docker-compose.yml with --profile prod instead:
#   docker compose --profile prod up -d
#
# Or use the build-prod.sh script which handles this automatically

services:
  startpage:
    extends:
      file: docker-compose.yml
      service: startpage-prod
    container_name: startpage
    ports:
      - "81:80"
